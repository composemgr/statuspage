# nginx proxy address - http://172.17.0.1:3007

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "5m"
    max-file: "1"

services:
  statuspage:
    image: statuspage/statuspage:latest
    container_name: statuspage-app
    restart: unless-stopped
    logging: *default-logging
    networks:
      - statuspage
    ports:
      - "3007:3007"
    environment:
      TZ: ${TZ:-America/New_York}
      PUID: ${SERVICE_USER:-1000}
      PGID: ${SERVICE_GROUP:-1000}
    volumes:
      - ./volumes/config/statuspage:/config
      - ./volumes/data/statuspage:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3007"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  statuspage:
    external: false
